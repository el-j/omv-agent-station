version: "1.0"
type: component
data:
  name: omv-services-agentstation-diagnostics-page
  type: formPage
  config:
    hints:
      - type: info
        text: _("<strong>📊 Real-Time Agent Station Diagnostic Center -</strong><br>Live container matrix, storage utilization, connected AI model gateways, and execution logs. Click <strong>Refresh Diagnostics &amp; Logs</strong> below for instant live telemetry.")
    request:
      service: AgentStation
      get:
        method: getDiagnostics
    fields:
      - type: divider
        title: _("1. Sandboxed Docker Containers & Services Status")
      - type: textarea
        name: container_status
        label: _("Live Container Matrix (Status, Memory & Ports)")
        readonly: true
        rows: 6
      - type: divider
        title: _("2. Storage & Sandboxed Workspace Utilization")
      - type: textarea
        name: storage_status
        label: _("Workspace, Obsidian Vault & Volume Capacity")
        readonly: true
        rows: 4
      - type: divider
        title: _("3. Connected AI Providers & Router Gateway")
      - type: textarea
        name: ai_status
        label: _("Active AI Model Endpoints & CLI Runtimes")
        readonly: true
        rows: 8
      - type: divider
        title: _("4. Live Provisioning & Docker Execution Logs")
      - type: textarea
        name: log_output
        label: _("Full Execution Stream (Image Downloads, Compose & Container Startup)")
        readonly: true
        rows: 18
    buttons:
      - text: _("Refresh Diagnostics & Logs")
        icon: "mdi:refresh"
        click:
          type: reload
